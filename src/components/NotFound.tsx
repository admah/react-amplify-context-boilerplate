import React from "react";

const NotFound: React.FC = () => {
  return (
    <div data-testid="NotFoundMessage">
      Sorry, but we could not find what you're looking for.
    </div>
  );
};

export default NotFound;
